# 評価

完成度はrailsのほうが上。

ログの表示などで、自前の実装がそれなりにある。

railsと違い、vite,tailwindが最初から使えるのはメリット。

いろいろと都合がいいPHPで動くのが強み。

## PHPとサーバーコスト

サーバーコストは、大体この順番。

オンプレミス > IaaS > PaaS > VPS > レンタルサーバー

PaaS、レンタルサーバーだけ、サーバーのセキュリティ対応が軽い。

レンタルサーバーでは、PHPが動く。

なぜなら、PHPは非常駐型なので、サーバーへの負荷が限定的になるため、レンタルサーバーでも採用しやすいから。

つまり、PHPだと、サーバーコストを限界まで下げられる。

## PHPがなぜ今でも都合がいいのか

メリットとデメリットを見ればわかると思う。

### メリット

- 非常駐型のため、サーバーが安定する。
- 非常駐型のため、スレッドセーフの知識がない人でも、安全に運用できる。
- 非常駐型での高速化が徹底してるため、小中規模の案件レベルだと、パフォーマンスの問題が起こることがめったにない。
- 使える人が多い。
- wordpressで使っている言語。
- レンタルサーバーで利用できる。

### デメリット

- 非常駐型のため、構造上、どうしても、パフォーマンスが悪くなる部分はある。

この条件下では、小中規模の案件では、都合がいい。

そして、世の中の多くは、小中規模の案件。

PHPから他の言語に移行すると、苦労することが多くなる理由は、PHPのメリットが他の言語にはない部分が多いから。

他の言語のデメリットの多さには、慣れていないと気付けない。

PHPは、初心者がWEBで安全に利用するのには、都合がいいことが多い。

そして、新しいエンジニアは全員初心者。

PHPは、ダメな言語と言われがちだけど、それは言語構造の話。

インフラや安全性に関しては、やたらと都合がいいことが多い。

## なぜ、PHPは非常駐型でも、速度が出るのか

非常駐型で高速化するための最適化が、徹底しているから。

- PHP自体が高速化している。
- opcacheで、コンパイル時間がなくなっている。
- php-fpmで、プロセス生成時間がなくなっている。

つまり、構造上は、メモリの移動とブートストラップの分くらいしか、常駐型との違いがない。



## PHPで利用してはいけない使い方

PHPは万能じゃないので、WebSocketサーバーでは使ってはいけないと思う。

このことは、PHP,Nodeの両方でWebSocketサーバーを作ってみれば、PHPが、このような処理には、どれほど向いていないかわかると思う。

## 常駐型実行環境のPHP（Swooleなど）

この場合は、速度は速くなるけど、非常駐型のメリットはすべてなくなる。

通常は、PHPエンジニアしかいない状態で、非常駐型では対処できなくなった場合でのみの利用になると思う。

ただ、これが必要な場合は、railsを採用したほうがいいと思う。

## Railsと比べたときのLaravelの問題点

地味にいろいろある。

### ORMのJoin

RailsのORMを使ったことがある人が、Laravelを使っていて不満を感じるのは、Joinの扱いだと思う。

一つの対策案は、Join用のスコープを用意すること。

これで、多少はすっきりする。

超高負荷DBではJoinは使えなくなるので、超高負荷DBでは、逆に、この問題がなくなる。

### Validationの定義が散らばる

モデルにまとめればいいと思う。

詳しくは、実装例を参照。

### OLD処理

Railsのような実装ができなくなるため、いろいろと問題が起きる場合がある。

状況に応じて、使わないというのも一つの手だと思う。

実装例に、railsライクな実装もあるので、詳しくは、そちらを参照。

実装例を見ればわかる通り、railsライクな実装のほうがすっきりまとまる。

OLD処理だと、曲芸的になる。

そして、複雑な導線になってくると、これがさらに曲芸的になり、手に負えなくなる人も増えてくると思う。

Island(Vue)の場合は、この問題はなくなる。

railsライクな実装も、完璧というわけじゃないので、複雑な導線になる部分は、最初から、Island(Vue)にするのが正解だと思う。

### ログ

実装例のように、自前で追加すればいい。

### 認証の細かい制御

実装例を参考にしてください。

## スレッドセーフの問題について

Railsではクラス変数、Nodeではグローバル変数とモジュール内変数は、リクエスト間で共有される。

そのため、別のユーザーの情報が表示される事故になることがある。

さらに、低い確率でのみ発生する不具合の場合もあり、その場合は、高負荷時に問題が発覚することもある。

PHPではリクエストごとにメモリがクリアされるため、この問題が起きる心配がない。（ただし、上記のSwooleなどの常駐型PHPの場合は別）

### PHPのメモリ管理の仕組み

#### php-fpmを使わない場合

リクエストごとに、プロセスが終了するため、当然ながら、メモリもクリアされる。

#### php-fpmを使う場合

リクエストが終わってもプロセスは残るけど、メモリはロジックを含めてクリアされて空になる。

### PHPから別言語へ移行したときに起きやすい問題

PHPエンジニアの場合は、スレッドセーフを意識しないでいいため、スレッドセーフの知識がない場合がある。

そのため、PHPエンジニアが、別言語に移行した直後は、この問題を発生させやすくなる。

事業をPHPから、別言語に移行した場合も、この問題で、想定外の苦労をすることになりやすい。
